<div class="container">
  <h1> <%= @list.name %></h1>

  <%= link_to "Back all lists <<", lists_path, class: "btn btn-primary" %>
  <%= link_to "Delete", list_path(@list), data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'}, class: 'btn btn-danger' %>
  <%= button_to "Destroy this list", @list, data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'}, class: 'btn btn-primary mt-3' %>
</div>
